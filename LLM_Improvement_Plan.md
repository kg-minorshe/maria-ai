# Дорожная карта для выхода на уровень Claude 4 Sonnet

Этот документ задаёт русскоязычный план действий, метрики и конфигурации, необходимые для вывода модели на качество уровня Claude 4 Sonnet. План разделён на цели, данные, архитектуру/обучение, выравнивание, оценку, абляции и деплой с операционализацией.

## 1) Цели и метрики
- **Целевые KPI (стиль Open LLM Leaderboard):**
  - Средний балл ≥ 82: MMLU ≥ 82, HellaSwag ≥ 88, GSM8K ≥ 92 (5-shot), BBH ≥ 82, WinoGrande ≥ 88.
  - **Перплексия:** WikiText103 ≤ 6; The Stack (код) ≤ 4.5.
  - **Фактуальность:** TruthfulQA ≥ 78; FEVER-precision ≥ 85%; галлюцинации < 8% на retrieval-grounded тестах.
  - **Безопасность:** ASR < 3%; корректные отказы ≥ 95% на jailbreak-наборах; токсичность < 0.5% (RealToxicityPrompts).
  - **Стабильность:** межзапусковой косинусный дрейф скрытых слоёв < 1e-3; контролируемая анизотропия через RMSNorm.
- **Размеры и контекст:**
  - Контекст 256k (обучение до 128k с YaRN/NTK-экстраполяцией, валидация до 256k).
  - Flagship: d_model 10 240, 80 слоёв, 80 голов (GQA 8 KV), d_ff 27k, токенайзер 128k, контекст 128k/256k.
  - Mid: d_model 6144, 48 слоёв, 48 голов (GQA 8), d_ff 16k, контекст 64k/128k.
- **Зоны превосходства:** рассуждение (GSM8K/BBH), мульти-язык (xWinograd/xStoryCloze), код (HumanEval/MBPP), устойчивость к вредным запросам.

## 2) Данные: сбор и нормализация
- **Микс языков (пример):** RU 70%, EN 30%.
- **Шаги очистки:**
  - Unicode NFC, чистка пробелов, выравнивание предложений/абзацев, шардирование документов до 2–4k токенов.
  - Детокс и PII: классификаторы + регекспы, блэклисты, детерминированное хэширование PII для аудита.
  - Дедуп: MinHash LSH, ограничение 13-gram overlap < 30%.
  - Фильтры качества: band-pass по перплексии (маленькая LM), длина/шаблоны/boilerplate, лицензионный вайтлист кода.
  - Антиконтаминация: хэширование эталонных бенчмарков, запрет пересечений с eval.
- **Баланс задач:**
  - **Reasoning/CoT:** пошаговые трейсеры 2–64 шага, маркировка промежуточных фактов.
  - **Инструкции:** self-instruct + дистилляция; мультилингвальные перефразы; теги стиля (кратко/пошагово/формально).
  - **Tool-use/RAG:** диалоги с функциями, строгие схемы вызовов, негативные примеры отказов.
  - **Код:** Python/JS/Java/C++/Go с тестами, докстрингами и трассами исполнения.
  - **Длинный контекст:** книги/мультимодульные наборы; sliding-window маскирование; сшивка контекстов retrieval.
- **Целевой объём SFT:** 5–8 млн диалоговых примеров; CoT 15–25%; tool-use 10%; код 15%; long-context QA 10%; остальное — общее общение.

## 3) Архитектура и обучение
- **Модель:** трансформер decoder-only, Pre-LN + RMSNorm, SwiGLU, Rotary (QK) с YaRN, GQA (8 KV), FlashAttention2; блок-спарс/скользящее окно для длинного контекста.
- **Оптимизация:**
  - AdamW (β1=0.9, β2=0.95, weight_decay=0.1), grad clip 1.0.
  - LR: косинус, разогрев 2–4k шагов; пик 2.5e-4 (34B) / 1.8e-4 (70B); финал 2.5e-6.
  - Батч: 3–4T токенов pretrain; глобальный 2–3M токенов (ZeRO-3/FSDP) с packing.
  - Точность: bfloat16 + FP8 GEMM; мастер-веса fp32; gradient checkpointing; activation offload при необходимости.
  - Регуляризация: dropout 0–0.1; stochastic depth 0–0.2; масштабирование attention logits для длинного контекста.
- **Фазы:**
  1. **(C)Pretrain** на очищенном корпусе: 10–20% код, 15% диалоги, 10% длинный контекст.
  2. **SFT**: баланс инструкций/CoT/tool-use; curriculum от коротких к длинным контекстам.
  3. **Стабилизация:** мониторинг всплесков лосса, авто-откат, контроль активаций и норм.

## 4) Выравнивание
- **Preference learning:** DPO/IPO на 0.5–1М пар (human+model) с акцентом на вежливость, точность ссылок, корректные отказы, сжатие при запросе краткости.
- **RLHF/RLAIF:** PPO/GRPO с KL-таргетом 0.5–1.0; отдельные reward-модели helpfulness/harmlessness; награда за корректный формат tool-use.
- **Безопасность:**
  - Фильтры генерации (safety classifier + allowlist критических доменов), шаблоны отказов, требование цитат для фактологии.
  - Red teaming: автоматическое генерирование атак + человек в контуре; быстрые патчи через SFT/LoRA.

## 5) Оценка и валидация
- **Бенчмарки:** MMLU, GSM8K, HumanEval, MBPP, HellaSwag, BBH, TruthfulQA, WinoGrande, XQUAD/xWinograd, LongBench/Needle-in-a-Haystack.
- **Режим запуска:** еженедельный полный прогон; по эпохам — спот-чек MMLU 5-shot и GSM8K 8-shot; аудиты галлюцинаций на retrieval-QA; оценка точности ссылок.
- **Цели:** достижение ±2% от Claude 4 Sonnet на GSM8K/BBH, HumanEval ≥ 92/MBPP ≥ 86, ASR < 3%.

## 6) Абляции и улучшения
- **Абляции:** CoT vs без CoT; длина CoT; tool-use vs без tool-use; контекст 32k/64k/128k; GQA vs MQA; sweep dropout/stochastic depth; доля кода 10/20/30%; влияние фильтрации и языкового баланса.
- **Усиления:** точечное дообучение на слабых доменах (математика, мультиязык, безопасность); RAG-тюнинг; LoRA/адаптеры для доменов; продолженный pretrain на редких языках и низкоресурсном коде.
- **Цикл:** Evaluate → Diagnose → Targeted data/SFT/RLHF patch → Re-evaluate, до достижения KPI.

## 7) Деплой, эффективность и наблюдаемость
- **Прод-инференс:** KV-кеш с paged attention; квантование AWQ/GPTQ/FP8; спекулятивное декодирование (драфт-модель); ранний выход при низкой энтропии; непрерывное батчирование.
- **Мониторинг:** дашборд качества (MMLU-срезы, отказоустойчивость, латентность), детекция дрейфа (распределения эмбеддингов), автоматический red teaming, shadow-deploy → канарейка → полное включение.
- **Экономика/операции:** лимиты стоимости по сеансу, авто-rollback, регулярное обновление preference-данных из прод-фидбэка.